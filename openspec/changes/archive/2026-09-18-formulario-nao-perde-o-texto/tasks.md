## 1. O texto volta

- [x] 1.1 `ContactDraft` nos resultados `invalid` e `failed`; verificar com `contact.test.ts`
- [x] 1.2 `ContactForm` refaz os cinco campos a partir do rascunho (select remontado pela chave) e foca o primeiro campo com erro; verificar no navegador com mensagem curta, e-mail inválido e falha de entrega
- [x] 1.3 Dica do campo de mensagem pede pelo menos uma frase; verificar com a sonda de largura em 320, 390, 412 e 768 px nas três rotas
- [x] 1.4 Erro com átomo violeta e peso forte em `globals.css`; texto do mínimo diz o motivo; verificar com `brand:check`, captura e sonda de largura
